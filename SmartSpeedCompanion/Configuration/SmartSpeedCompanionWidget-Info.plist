<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!--
      WidgetKit bundle Info.plist. The NSExtension dictionary is the
      minimum needed for Xcode to recognize the bundle as a WidgetKit
      extension target. The principal class is left implicit so
      WidgetKit discovers `@main struct SpeedWidgetBundle` at link time.
      NSSupportsLiveActivities is required for the
      ActivityConfiguration(for: SpeedActivityAttributes.self) inside
      `SpeedLiveActivityView` to render under iOS 16.1+.
    -->
    <key>CFBundleDisplayName</key>
    <string>Speedio</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
    <key>CFBundleShortVersionString</key>
    <string>$(MARKETING_VERSION)</string>
    <key>CFBundleVersion</key>
    <string>$(CURRENT_PROJECT_VERSION)</string>
    <key>NSExtension</key>
    <dict>
        <key>NSExtensionPointIdentifier</key>
        <string>com.apple.widgetkit-extension</string>
    </dict>
    <key>NSSupportsLiveActivities</key>
    <true/>
</dict>
</plist>
